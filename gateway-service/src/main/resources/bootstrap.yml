spring:
  application:
    name: gateway-service
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      profile: default
      label: master
      fail-fast: true
      retry:
        initial-interval: 1000
        max-attempts: 6
        max-interval: 2000
        multiplier: 1.1